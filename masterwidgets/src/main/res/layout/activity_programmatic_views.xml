<?xml version="1.0" encoding="utf-8"?>
<ScrollView
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/material_keyline_2">

    <LinearLayout
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:id="@+id/ll_programmatic_views"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:divider="@drawable/transparent_divider"
            android:orientation="vertical">

        <LinearLayout android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:orientation="horizontal"
                      android:gravity="center_vertical">

            <EditText android:id="@+id/et_programmatic_views_label"
                      android:layout_weight="1"
                      android:layout_width="0dp"
                      android:lines="3"
                      android:maxLines="4"
                      android:inputType="textMultiLine"
                      android:imeOptions="actionDone"
                      android:layout_height="wrap_content"/>

            <Button android:id="@+id/btn_programmatic_views_generate"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginLeft="5dp"
                    android:layout_marginStart="5dp"
                    android:gravity="center"
                    android:text="@string/create"/>
        </LinearLayout>

        <!-- Views will be added here programmatically -->

    </LinearLayout>
</ScrollView>
